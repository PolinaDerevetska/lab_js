/* Напишіть стрілкову функцію, яка
перевіряє, чи містить масив хоча б одне від'ємне число, використовуючи .some(). */

const negative = arr => arr.some(num => num < 0);
console.log(negative([1, 2, -3, 4]));